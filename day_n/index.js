let FS = require('fs');

let input = FS.readFileSync(__dirname + '/input.txt').toString()
    .split('\r\n')
    .map((line) => {
        return line
            // .split(' ')
    })


// let key = input[0];
// input = input.slice(1);

let flag = null

//LOGIC
input.forEach((l, i) => {
    l.forEach((p, j) => {

    })
})


console.log(flag)
